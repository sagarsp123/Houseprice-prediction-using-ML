<% Object.keys(messages).forEach(function (type) { %>

<div class="alert alert-<%= type %> alert-dismissal">

<button type="button" class="close" data-dismiss="alert">&times;</button>

  <% messages[type].forEach(function (message) { %>
    <%= message %>
  <% }) %>
 </div>
<% }) %>